<idea-plugin>
    <idea-version since-build="233" />

    <id>dev.panuszewski.gradle-jumper</id>

    <name>Gradle Jumper</name>

    <vendor email="radoslaw.panuszewski15@gmail.com">Radosław Panuszewski</vendor>

    <description><![CDATA[
        <p>Gradle is recently introducing more and more typesafe accessors:</p>
        <ul>
            <li>version catalog accessors (for example <tt>libs.kotlin.stdlib</tt>)</li>
            <li>precompiled script plugin accessors (for example <tt>`my-custom-plugin`</tt>)</li>
        </ul>

        <p>It is sometimes tempting to <tt>Ctrl/Cmd + Click</tt> on such accessor and expect to jump into the right place.
        Instead, IDE leads us to the generated code which makes up the accessor.</p>

        <p>This plugin is here to help! Check out the quick demo above ;)</p>
    ]]>
    </description>

    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.kotlin</depends>
    <depends>org.intellij.groovy</depends>

    <extensions defaultExtensionNs="com.intellij">
        <gotoDeclarationHandler implementation="dev.panuszewski.gradle.jumper.GoToScriptPluginHandler"/>
        <gotoDeclarationHandler implementation="dev.panuszewski.gradle.jumper.GoToSubprojectHandler"/>
    </extensions>

</idea-plugin>